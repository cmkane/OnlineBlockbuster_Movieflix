I split the downloads into 10 different parts, so should be 10 of each screenshot. (45471 each) 
The files starting with "Postman" show the getting movies endpoint being called and how many movies is returned in response.

Note that movies were added to the tables movies2, genres2, and genres_in_movies2 in the colin-baker database. (So that it doesn't affect HW3 grading).
